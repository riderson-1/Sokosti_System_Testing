# Enable the nRF5340 network core IPC radio image for Bluetooth HCI.
# This file is consumed by sysbuild, not by the application image.

config NRF_DEFAULT_IPC_RADIO
	default y

config NETCORE_IPC_RADIO_BT_HCI_IPC
	default y

source "share/sysbuild/Kconfig"
